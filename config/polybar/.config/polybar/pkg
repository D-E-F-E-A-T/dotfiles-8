#!/bin/bash
# Description: Check pacman updates.

. $HOME/.local/bin/color-parser

pac=$(checkupdates | wc -l)

if [[ -n "$pac" ]];then
        echo "%{F$WHITE}$pac %{F-}"
else
        echo "%{F#GREEN} 0 %{F-}"
fi
